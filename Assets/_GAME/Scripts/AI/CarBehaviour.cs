using System.Collections;
using System.Collections.Generic;
using _GAME.Scripts.AI.Base;
using UnityEngine;

public class CarBehaviour : BaseAI
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
